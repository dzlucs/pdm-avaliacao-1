/* import { Selector } from "testcafe";

fixture("Home Page").page("http://localhost:8081");

test("A página inicial carrega corretamente", async (t) => {
  // Verifica se o texto 'Welcome' está na tela
  await t.expect(Selector("text").withText("Welcome").exists).ok();

  // Verifica se o botão 'Developer' está na tela
  await t.expect(Selector("text").withText("Developer").exists).ok();

  // Verifica se o botão 'Student' está na tela
  await t.expect(Selector("text").withText("Student").exists).ok();

  //exemplos alternativos
  await t.expect(Selector("span").withText("Welcome").exists).ok();
  await t.expect(Selector("span").withText("Developer").exists).ok();
  await t.expect(Selector("span").withText("Student").exists).ok();
});
// Você pode adicionar outros testes aqui, por exemplo:
// await t.expect(Selector('button').withText('Entrar').exists).ok();
 */
